node_modules/
dist/
build/
.env
backend.env
*.log
.DS_Store

# Anexos de OS enviados em runtime (fotos/laudos) — binários locais
backend/uploads/

.vercel

# Notas locais do assistente (memória de sessão) — não versionar
.claude/
frontend/yarn.lock
